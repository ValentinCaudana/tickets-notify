# 🎟️ Tickets Notify

A mini app that gathers all information about football ticket sales in one place — including instructions, official links, and sale reminders

## 🚀 Stack

- Node.js + Express
- Frontend static (HTML + JS)
- JSON storage (for now)

## ⚙️ Setup local

```bash
git clone https://github.com/ValentinCaudana/tickets-notify.git
cd tickets-notify
npm install
npm run dev
```
